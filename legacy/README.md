# Legacy Files

This folder contains archived, non-active scripts kept only for reference.

- `script_en_js.legacy.js` (old monolithic implementation)

Active implementation for `4.html` now lives in `js/app/` and starts from `js/app/main.js`.
